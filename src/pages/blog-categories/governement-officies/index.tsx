import { Typography } from "@mui/material";
const index = () => {
  return (
    <div>
      <Typography variant="h2" textAlign={"center"}>
        index for government offices
      </Typography>
    </div>
  );
};

export default index;
